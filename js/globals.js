/**
 * Holds the starting timeStamp of every jump animation.
 * @var {{number}}
 */
let startJump;
/**
 * Holds the starting timeStamp of every throw animation.
 * @var {{number}}
 */
let startThrow;
/**
 * Holds the starting timeStamp of the drawLoop function.
 * @var {{number}}
 */
let startDraw;

export { startJump, startThrow, startDraw };
